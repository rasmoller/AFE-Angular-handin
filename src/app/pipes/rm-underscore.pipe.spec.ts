import { RmUnderscorePipe } from './rm-underscore.pipe';

describe('RmUnderscorePipe', () => {
  it('create an instance', () => {
    const pipe = new RmUnderscorePipe();
    expect(pipe).toBeTruthy();
  });
});
